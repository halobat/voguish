# ----------- CONFIGURABLE START -----------
title:            "VOGUISH"
description:      "Such theme, very cool."
url:              https://halobat.github.io/voguish # Site url, without a slash at the end.
signoff:          "VOGUISH" # Your name
facebook_appid:   # APP ID for like button, remove from /_includes/share.html if unwanted.
google_analytics: # Set tracking, remove from /javascripts/basic.js if unwanted.
share:            false # Turn on twitter and facebook share buttons

links:
  - name:         Posts
    url:          /
    external:     false
  - name:         About me
    url:          /about
    external:     false
  - name:         Jeans
    url:          /jeans
    external:     false
# ------------ CONFIGURABLE END ------------

permalink:        /posts/:title
defaults:
  - values:
      layout: "default"
   - scope:
      path: ""
      type: "jeans"
    values:
    layout: "catloop"


collections:
      jeans:
      output: true
      
      
defaults:

